export const DB_URL = 'sqlite:seochang-operations.db';

export const migrations = [
  `CREATE TABLE IF NOT EXISTS people (
    id TEXT PRIMARY KEY,
    kind TEXT NOT NULL,
    name TEXT NOT NULL,
    role TEXT NOT NULL DEFAULT '',
    category TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL DEFAULT '재직',
    started_at TEXT NOT NULL DEFAULT '',
    ended_at TEXT NOT NULL DEFAULT '',
    email TEXT NOT NULL DEFAULT '',
    duty_text TEXT NOT NULL DEFAULT '',
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    sync_status TEXT NOT NULL DEFAULT 'pending'
  )`,
  `CREATE TABLE IF NOT EXISTS attendance (
    id TEXT PRIMARY KEY,
    person_id TEXT NOT NULL,
    person_kind TEXT NOT NULL,
    date TEXT NOT NULL,
    year_month TEXT NOT NULL,
    status TEXT NOT NULL,
    memo TEXT NOT NULL DEFAULT '',
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    synced_at TEXT NOT NULL DEFAULT '',
    UNIQUE(person_id, date)
  )`,
  `CREATE TABLE IF NOT EXISTS children (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    gender TEXT NOT NULL DEFAULT '',
    phone TEXT NOT NULL DEFAULT '',
    resident_no TEXT NOT NULL DEFAULT '',
    birth_date TEXT NOT NULL DEFAULT '',
    age TEXT NOT NULL DEFAULT '',
    school TEXT NOT NULL DEFAULT '',
    grade TEXT NOT NULL DEFAULT '',
    address TEXT NOT NULL DEFAULT '',
    use_type TEXT NOT NULL DEFAULT '',
    income_level TEXT NOT NULL DEFAULT '',
    guardian_name TEXT NOT NULL DEFAULT '',
    guardian_relation TEXT NOT NULL DEFAULT '',
    family_type TEXT NOT NULL DEFAULT '',
    guardian_contact TEXT NOT NULL DEFAULT '',
    vulnerable_type TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL DEFAULT '재원',
    joined_at TEXT NOT NULL DEFAULT '',
    left_at TEXT NOT NULL DEFAULT '',
    manager TEXT NOT NULL DEFAULT '',
    kids_id TEXT NOT NULL DEFAULT '',
    memo TEXT NOT NULL DEFAULT '',
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    sync_status TEXT NOT NULL DEFAULT 'pending'
  )`,
  `CREATE TABLE IF NOT EXISTS child_attendance (
    id TEXT PRIMARY KEY,
    child_id TEXT NOT NULL,
    date TEXT NOT NULL,
    year_month TEXT NOT NULL,
    status TEXT NOT NULL,
    memo TEXT NOT NULL DEFAULT '',
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    synced_at TEXT NOT NULL DEFAULT '',
    UNIQUE(child_id, date)
  )`,
  `CREATE TABLE IF NOT EXISTS child_year_records (
    id TEXT PRIMARY KEY,
    child_id TEXT NOT NULL,
    year INTEGER NOT NULL,
    name TEXT NOT NULL,
    gender TEXT NOT NULL DEFAULT '',
    phone TEXT NOT NULL DEFAULT '',
    resident_no TEXT NOT NULL DEFAULT '',
    birth_date TEXT NOT NULL DEFAULT '',
    age TEXT NOT NULL DEFAULT '',
    school TEXT NOT NULL DEFAULT '',
    grade TEXT NOT NULL DEFAULT '',
    address TEXT NOT NULL DEFAULT '',
    use_type TEXT NOT NULL DEFAULT '',
    income_level TEXT NOT NULL DEFAULT '',
    guardian_name TEXT NOT NULL DEFAULT '',
    guardian_relation TEXT NOT NULL DEFAULT '',
    family_type TEXT NOT NULL DEFAULT '',
    guardian_contact TEXT NOT NULL DEFAULT '',
    vulnerable_type TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL DEFAULT '재원',
    joined_at TEXT NOT NULL DEFAULT '',
    left_at TEXT NOT NULL DEFAULT '',
    manager TEXT NOT NULL DEFAULT '',
    kids_id TEXT NOT NULL DEFAULT '',
    memo TEXT NOT NULL DEFAULT '',
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    sync_status TEXT NOT NULL DEFAULT 'pending',
    UNIQUE(child_id, year)
  )`,
  `CREATE TABLE IF NOT EXISTS journals (
    id TEXT PRIMARY KEY,
    date TEXT NOT NULL UNIQUE,
    operating_hours TEXT NOT NULL DEFAULT '',
    manager TEXT NOT NULL DEFAULT '',
    capacity INTEGER NOT NULL DEFAULT 35,
    enrolled INTEGER NOT NULL DEFAULT 0,
    present_children INTEGER NOT NULL DEFAULT 0,
    absent_children INTEGER NOT NULL DEFAULT 0,
    staff_count INTEGER NOT NULL DEFAULT 0,
    teacher_count INTEGER NOT NULL DEFAULT 0,
    public_service_count INTEGER NOT NULL DEFAULT 0,
    other_visitor_count INTEGER NOT NULL DEFAULT 0,
    guidance_text TEXT NOT NULL DEFAULT '',
    staff_text TEXT NOT NULL DEFAULT '',
    child_text TEXT NOT NULL DEFAULT '',
    visitor_text TEXT NOT NULL DEFAULT '',
    facility_text TEXT NOT NULL DEFAULT '',
    work_text TEXT NOT NULL DEFAULT '',
    other_text TEXT NOT NULL DEFAULT '',
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    sync_status TEXT NOT NULL DEFAULT 'pending'
  )`,
  `CREATE TABLE IF NOT EXISTS sync_jobs (
    id TEXT PRIMARY KEY,
    target TEXT NOT NULL,
    payload_json TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'pending',
    created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
    finished_at TEXT NOT NULL DEFAULT '',
    error TEXT NOT NULL DEFAULT ''
  )`,
  `CREATE TABLE IF NOT EXISTS app_settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL DEFAULT '',
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS journal_templates (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    group_name TEXT NOT NULL DEFAULT '일지',
    html TEXT NOT NULL DEFAULT '',
    is_default INTEGER NOT NULL DEFAULT 0,
    updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE INDEX IF NOT EXISTS idx_people_kind_status ON people(kind, status)`,
  `CREATE INDEX IF NOT EXISTS idx_attendance_year_month ON attendance(year_month)`,
  `CREATE INDEX IF NOT EXISTS idx_children_status ON children(status, joined_at)`,
  `CREATE INDEX IF NOT EXISTS idx_child_attendance_year_month ON child_attendance(year_month)`,
  `CREATE INDEX IF NOT EXISTS idx_child_attendance_child_date ON child_attendance(child_id, date)`,
  `CREATE INDEX IF NOT EXISTS idx_child_year_records_child_year ON child_year_records(child_id, year)`,
  `CREATE INDEX IF NOT EXISTS idx_journals_date ON journals(date)`,
  `CREATE INDEX IF NOT EXISTS idx_journal_templates_group ON journal_templates(group_name, is_default)`
];
