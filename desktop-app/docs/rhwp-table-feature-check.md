# RHWP 표 기능 점검 기록

이 파일은 RHWP 표 기능을 수정할 때마다 반드시 갱신한다.

## 업데이트 규칙

- 표 기능을 수정하면 작업 완료 전에 이 파일을 갱신한다.
- 갱신 시각은 `yyyy-MM-dd HH:mm:ss +09:00` 형식으로 남긴다.
- 표 기능 점검 결과는 `정상`, `부분 정상`, `미해결`, `보류` 중 하나로 기록한다.
- 브라우저에서 직접 확인한 내용과 아직 확인하지 못한 내용을 분리해서 적는다.

## 기본 점검 항목

| 항목 | 확인 내용 |
| --- | --- |
| 표 클릭 | 표 셀 안쪽 클릭 시 커서가 셀 내부로 들어가는지 확인 |
| 표 메뉴 | 표 메뉴가 비활성화되지 않고 열리는지 확인 |
| 셀 속성 | 셀 속성 메뉴가 활성화되는지 확인 |
| 행 추가 | 위/아래 행 추가 메뉴가 활성화되는지 확인 |
| 열 추가 | 왼쪽/오른쪽 열 추가 메뉴가 활성화되는지 확인 |
| 표 선택 | 표 전체 선택과 셀 선택이 충돌하지 않는지 확인 |
| 표 이동 | 표가 편집 용지 여백 밖으로 밀리지 않는지 확인 |
| 표 크기 | 표 너비/높이 조절이 문서 영역 안에서 동작하는지 확인 |

## 점검 기록

### 2026-05-21 21:22:26 +09:00

작업:
- RHWP 원본 위치를 `https://github.com/edwardkim/rhwp.git`로 확인하고, LFS 대용량 PDF는 건너뛰는 방식으로 `desktop-app/vendor/rhwp/rhwp-studio` 로컬 체크아웃을 복원했다.
- 운영 앱의 RHWP 보기 상단에 표 미세 이동, 위치 적용/초기화, 표 폭 맞춤, 보기 폭 맞춤 도구를 추가했다.
- RHWP iframe 로드/표 도구 조작 후 iframe 포커스를 복원해 표 셀 클릭 뒤 키보드 입력 흐름이 끊길 가능성을 줄였다.
- 점검 화면의 계산식 판정을 `table:formula`뿐 아니라 `table:block-formula`, `table:block-sum`, `table:block-avg`, `table:block-product`도 함께 보도록 정리했다.
- 현재 `index.html`이 실제로 불러오는 RHWP 번들에 표 이동 경계 제한 가드가 적용되는지 빌드 가드가 검사하도록 수정했다.
- `moveTables` API 경로도 RHWP 내부 표 이동 경계 제한 함수를 거치도록 활성 번들을 보정했다.

점검 결과:

| 항목 | 상태 | 메모 |
| --- | --- | --- |
| RHWP 원본 위치 | 정상 | upstream과 로컬 체크아웃 경로 확인. `desktop-app/vendor/rhwp/`는 Git 제외 경로이므로 커밋하지 않음 |
| 표 이동 UI | 정상 | 운영 앱에서 오른쪽 1mm 이동 후 상태가 `표 2개 · 위치 283, 0`으로 갱신되고, 위치 초기화가 `0, 0`으로 복귀함 |
| 표 크기 UI | 부분 정상 | `표 폭 맞춤` 버튼으로 템플릿 표를 다시 로드하는 흐름 확인. 실제 셀 경계 드래그는 RHWP 내부 기능에 의존 |
| 직접 타이핑 포커스 | 부분 정상 | 표 도구 조작과 폭 맞춤 후 RHWP iframe 포커스 유지 확인. 실제 셀 내부 타이핑은 후속 수동 검증 필요 |
| 계산식 판정 | 정상 | 점검 화면이 실제 노출된 블록 계산식 명령까지 포함해 판정하도록 수정 |
| 경계 가드 | 정상 | 빌드 가드가 활성 번들의 `dt`/`pt` 경계 제한 함수와 `moveTables` 연결을 확인함 |

브라우저 확인:
- `http://127.0.0.1:1420` 템플릿 만들기 화면에서 새 표 도구 표시 확인
- 오른쪽 1mm 이동, 위치 초기화, 표 폭 맞춤 버튼 동작 확인
- `http://127.0.0.1:1420/rhwp-studio/table-feature-lab.html` 점검 화면 로드 확인, 계산식 판정 문구 확인

빌드 확인:
- `npm run build` 통과
- 참고용 upstream 원본 소스에는 Seochang 표 경계 패치가 없어서 가드가 경고만 출력함. 배포 번들은 정상 검사 대상

### 2026-05-21 20:27:08 +09:00

작업:
- 표 메뉴와 상단 툴바에 `셀 범위 선택` 명령을 추가했다.
- 셀 선택 모드에서 일반 좌클릭이 선택 종료가 아니라 클릭한 셀까지 범위 확장으로 동작하도록 수정했다.
- Ctrl/Meta 클릭은 기존처럼 선택 제외/복원 토글로 유지했다.
- 점검 화면 문구를 `셀 범위 선택 시작` 흐름에 맞춰 갱신했다.

점검 결과:

| 항목 | 상태 | 메모 |
| --- | --- | --- |
| 셀 내부 포커스 | 정상 | RHWP 표 셀 클릭 후 셀 내부 상태로 전환 확인 |
| 셀 범위 선택 시작 | 정상 | 상단 툴바 `셀 선택` 버튼이 표시되고 실행 가능함을 확인 |
| 마우스 범위 확장 | 정상 | 셀 범위 선택 시작 후 다른 셀 일반 클릭으로 범위가 `1,2 → 8,3`까지 확장되는 것 확인 |
| 셀 병합 | 정상 | 다중 셀 범위 선택 후 `table:cell-merge` 명령 활성화 확인 |
| 셀 나누기 | 정상 | 셀 내부 포커스 후 `table:cell-split` 명령 활성화 확인 |
| 표/셀 속성 | 정상 | 셀 내부 포커스 후 셀 속성 명령 활성화 확인 |
| 행/열 추가 | 정상 | 위/아래 행, 왼쪽/오른쪽 열 추가 명령 활성화 확인 |
| 행/열 삭제 | 정상 | 행/열 삭제 명령 활성화 확인 |

브라우저 확인:
- `http://127.0.0.1:1420/rhwp-studio/table-feature-lab.html`
- `셀 범위 선택 시작` 실행 후 표 안 다른 셀을 일반 클릭해 셀 범위가 바뀌는지 확인
- 상단 툴바 `셀 선택` 버튼 클릭 후 병합 가능 범위가 만들어지는 것 확인

### 2026-05-21 20:04:47 +09:00

작업:
- RHWP iframe에 셀 범위 선택을 만드는 `startCellRangeSelection` 진단 API를 추가했다.
- `getEditorContext` 응답에 셀 선택 범위, 선택 단계, 다중 셀 여부를 포함했다.
- 점검 화면의 셀 나누기 명령 ID를 실제 명령인 `table:cell-split`으로 바로잡았다.
- 점검 화면에 `셀 범위 선택 점검` 버튼을 추가해 병합 조건을 버튼 한 번으로 확인할 수 있게 했다.

점검 결과:

| 항목 | 상태 | 메모 |
| --- | --- | --- |
| 셀 내부 포커스 | 정상 | 표 셀 안쪽 클릭 후 셀 내부 상태로 전환 확인 |
| 셀 나누기 | 정상 | 셀 내부 포커스 후 `table:cell-split` 명령 활성화 확인 |
| 셀 병합 | 정상 | 셀 범위 선택 점검 후 `1,1 → 1,2` 다중 셀 범위 선택 및 병합 명령 활성화 확인 |
| 표/셀 속성 | 정상 | 셀 내부 포커스 후 셀 속성 명령 활성화 확인 |
| 행/열 추가 | 정상 | 위/아래 행, 왼쪽/오른쪽 열 추가 명령 활성화 확인 |
| 행/열 삭제 | 정상 | 행/열 삭제 명령 활성화 확인 |
| 셀 높이/너비 같게 | 정상 | 높이/너비 같게 명령 활성화 확인 |
| 계산식 | 정상 | 계산식 명령 활성화 확인 |

브라우저 확인:
- `http://127.0.0.1:1420/rhwp-studio/table-feature-lab.html`
- 표 셀 클릭 후 셀 내부 포커스, 셀 나누기, 주요 표 명령 활성화 확인
- `셀 범위 선택 점검` 실행 후 셀 병합이 `가능`으로 전환되는 것 확인

### 2026-05-21 09:47:24 +09:00

작업:
- RHWP iframe에 현재 편집 컨텍스트와 표 명령 활성 상태를 읽는 `getEditorContext` API를 추가했다.
- 표 기능 점검 화면이 셀 클릭 전/후 상태를 실시간으로 판정하도록 수정했다.
- 셀 내부 포커스가 잡힌 뒤 실제 표 명령들이 활성화되는지 브라우저에서 재확인했다.

점검 결과:

| 항목 | 상태 | 메모 |
| --- | --- | --- |
| 셀 내부 포커스 | 정상 | 표 셀 안쪽 클릭 후 `현재 커서가 표 셀 내부 상태`로 전환 확인 |
| 표/셀 속성 | 정상 | 셀 내부 포커스 후 셀 속성 명령 활성화 확인 |
| 행 추가 | 정상 | 위/아래 행 추가 명령 활성화 확인 |
| 열 추가 | 정상 | 왼쪽/오른쪽 열 추가 명령 활성화 확인 |
| 행/열 삭제 | 정상 | 행/열 삭제 명령 활성화 확인 |
| 셀 높이/너비 같게 | 정상 | 높이/너비 같게 명령 활성화 확인 |
| 계산식 | 정상 | 계산식 명령 활성화 확인 |
| 셀 병합 | 보류 | F5 셀 선택 모드와 범위 선택 조건을 별도로 점검해야 함 |
| 셀 나누기 | 부분 정상 | 표 셀 포커스는 잡히지만 명령 실행 조건은 추가 확인 필요 |

브라우저 확인:
- `http://127.0.0.1:1420/rhwp-studio/table-feature-lab.html`
- 표 셀 클릭 전: 셀 내부 포커스 미진입 상태로 표시
- 표 셀 클릭 후: 셀 내부 포커스와 주요 표 명령 활성화 확인

### 2026-05-21 09:24:22 +09:00

작업:
- RHWP 표 안쪽을 클릭해도 셀 컨텍스트로 들어가지 못하던 문제를 보정했다.
- 표 셀 영역을 직접 찾아 커서를 셀 내부 위치로 이동시키는 방식으로 처리했다.
- 새 RHWP 번들을 빌드해 `desktop-app/public/rhwp-studio`에 반영했다.

점검 결과:

| 항목 | 상태 | 메모 |
| --- | --- | --- |
| 표 클릭 | 정상 | 셀 안쪽 클릭 후 표 컨텍스트가 잡히는 것을 확인 |
| 표 메뉴 | 정상 | 표 메뉴가 활성화되는 것을 확인 |
| 셀 속성 | 정상 | `table:cell-props` 메뉴 활성화 확인 |
| 행 추가 | 정상 | `table:insert-row-above`, `table:insert-row-below` 활성화 확인 |
| 열 추가 | 정상 | `table:insert-col-left`, `table:insert-col-right` 활성화 확인 |
| 표 선택 | 보류 | 다음 표 선택/이동 개선 작업에서 재확인 필요 |
| 표 이동 | 보류 | 편집 용지 여백 제한 로직과 함께 재점검 필요 |
| 표 크기 | 보류 | 표 크기 조절 기능 개선 시 재점검 필요 |

브라우저 확인:
- `http://127.0.0.1:1420/rhwp-studio/table-feature-lab.html`
- 새 번들 `assets/index-D4cZ2n_o.js` 로드 확인
- 셀 안쪽 클릭 후 표 관련 메뉴 활성화 확인
